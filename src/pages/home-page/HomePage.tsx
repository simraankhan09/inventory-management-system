import MainTemplate from "../../templates/MainTemplate";

const HomePage = () => {
  return (
    <MainTemplate className="flex items-center justify-center h-full">
      <div className="text-center font-semibold capitalize w-full mt-5">
        <h1>Get Start With</h1>
        <h3>Inventory Managment System</h3>
      </div>
    </MainTemplate>
  );
};

export default HomePage;
